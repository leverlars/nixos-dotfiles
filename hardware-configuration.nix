# Do not modify this file!  It was generated by ‘nixos-generate-confi>
# and may be overwritten by future invocations.  Please make changes
# to /etc/nixos/configuration.nix instead.
{ config, lib, pkgs, modulesPath, ... }:

{
  imports =
    [ (modulesPath + "/installer/scan/not-detected.nix")
    ];

  boot.initrd.availableKernelModules = [ "xhci_pci" "nvme" "usb_stora>
  boot.initrd.kernelModules = [ ];
  boot.kernelModules = [ "kvm-intel" ];
  boot.extraModulePackages = [ ];

  fileSystems."/" =
    { device = "/dev/disk/by-uuid/e5425b75-2d20-4172-8684-d572dd44671>
      fsType = "ext4";
    };

  boot.initrd.luks.devices."luks-746a0e28-15ae-4ac7-a7dd-08380c6cba45>

  fileSystems."/boot" =
    { device = "/dev/disk/by-uuid/A419-464C";
      fsType = "vfat";
      options = [ "fmask=0077" "dmask=0077" ];
    };

  swapDevices = [ ];

  # Enables DHCP on each ethernet and wireless interface. In case of >
  # (the default) this is the recommended approach. When using system>
  # still possible to use this option, but it's recommended to use it>
  # with explicit per-interface declarations with `networking.interfa>
  networking.useDHCP = lib.mkDefault true;
  # networking.interfaces.enp0s31f6.useDHCP = lib.mkDefault true;
  # networking.interfaces.wlp0s20f3.useDHCP = lib.mkDefault true;

  nixpkgs.hostPlatform = lib.mkDefault "x86_64-linux";
  hardware.cpu.intel.updateMicrocode = lib.mkDefault config.hardware.>
}
